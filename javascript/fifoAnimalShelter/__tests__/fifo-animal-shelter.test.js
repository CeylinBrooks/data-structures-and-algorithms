'use strict';

const AnimalShelter = require('./fifo-animal-shelter.js');

describe('Working Test', () => {
  it('Working test', () => {
    expect(true).toBeTruthy();
  });
});

describe('Animal Shelter class working', () => {
  let animalOne = {
    type: 'dog',
  };

  let animalTwo = {
    type: 'cat',
  }
})